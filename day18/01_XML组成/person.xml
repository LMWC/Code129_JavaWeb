<?xml version="1.0" encoding="utf-8" ?>
<!-- 注释：快捷键：ctrl+/ -->
<!--
1.文档声明
    格式:<?xml version="1.0" encoding="utf-8" ?>
    注意事项:
        1.文档声明是非必须的,但是建议加上
        2.文档声明必须为<?xml开头,以?>结束
        3. 文档声明必须从文档的1行1列位置开始【规范】
        4. 文档声明中常见的两个属性：
            version：指定XML文档版本。必须属性,这里一般选择1.0；
            enconding：指定当前文档的编码,可选属性,默认值是utf-8；。

2.元素element (标签)
    1. 元素是XML中最重要的组成部分，元素也叫标签
    2. 标签分为开始标签和结束标签，开始标签<名字>   结束标签</名字>
    3. 开始标签和结束标签中间写的是标签内容，标签的内容可以是文本，也可以是其他标签
    4. 如果标签没有任何内容，那么可以定义空标签(比如：<名字/>)
    5. 标签可以嵌套,但是不能乱嵌套
    6. 一个XML文件只有一个根标签
    7. 命名规则：
         不要使用XML  xML xml 写样的单词
         不能使用空格，冒号
         命名区分大小写
         数字不能开头
       总而言之：标签的命名你就正常的写一个单词或拼音就可以了
3.属性
    格式:<person 属性名="属性值"></person>
    注意事项:
        1. 属性是标签的一部分,它必须出现在开始标签中  用于修饰标签的
        2. 属性的定义格式：属性名=“属性值”,其中属性值必须使用单引或双引号括起来
        3. 一个元素可以有0~N个属性,但一个元素中不能出现同名属性
        4. 属性名不能使用空格 , 不要使用冒号等特殊字符,建议以字母开头
4.转义字符
    介绍:
        在xml中,一些符号被文档结构使用,想要在元素体或属性值使用这些符号,会造成冲突。
        为了避免这些冲突,定义了表示这些符号的字符,以便在元素体或属性值中使用。
        这些字符称为转义字符,也叫实体字符。XML中的转义字符与HTML中的一样。
    常见转义字符:
        1. 小于号(<)：===>   &lt;
        2. 大于号(>)：===>   &gt;
        3. 与符号(&)：===>   &amp;
        4. 单引号(‘)：===>    &apos;
        5. 双引号(“)：===> &quot;
    注意：严格讲,在XML中仅有字符 "<"和"&"是非法的。省略号、单双引号和大于号是合法的,但将其替换为实体引用是个好习惯。
5.字符区
    介绍:CDATA 内部的所有东西都会被解析器直接当做文本看待。
    格式:<![CDATA[文本数据]]>
    快捷键: CD

-->

<!--XML文档必须有一个根标签 不能为0个也不能有多个-->
<persons>
    <!--标签正常嵌套-->
    <person>
        <name>zs</name>
        <age>18</age>
    </person>

    <!--空标签-->
    <person></person>
    <!--空标签可以用一个自闭合标签表示-->
    <person />

    <!--错误示范-标签乱嵌套 指的是交叉嵌套-->
    <!--<person>
        <name>
        </person>
    </name>-->

    <!--错误示范-XML区分大小写    person!=Person-->
    <!--<person>zhangsan</Person>-->

    <!--标签名称可以用中文 但是不建议-->
    <人></人>

    <!--属性-->
    <person name="ww" age='20' />

    <!--转义字符-->
    <person>
        <name>zhaoliu</name>
        <price> >500 &amp;&amp; &lt;1000 </price>
        <price>
            <![CDATA[
                >500 && <1000  >500 && <1000 >500 && <1000 >500 && <1000  >500 && <1000
            ]]>
        </price>
        <price>
            <![CDATA[ >500 && <1000  >500 && <1000 >500 && <1000 >500 && <1000  >500 && <1000 ]]>
        </price>
    </person>
</persons>
